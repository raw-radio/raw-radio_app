// Web uses src/hooks/useAudioPlayer.web.ts (plain HTML <audio>).
// react-native-track-player is not used on web — importing it would pull
// shaka-player into the browser bundle. This is an intentional no-op.
export {}
